server:
  port: 0
discovery:
  services:
    stats-server-id: stat-server
spring:
  jpa:
    hibernate:
      ddl-auto: none
      format_sql: true
  sql:
    init.mode: always
  datasource:
    url: jdbc:postgresql://localhost:5432/ewm-main
    username: root
    password: root
